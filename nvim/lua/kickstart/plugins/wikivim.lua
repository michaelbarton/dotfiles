return {
  'lervag/wiki.vim', -- Vimwiki setup for zettelkasten

  config = function()
    -- vim.keymap.set('n', '<leader>sn', function()
    -- vimwiki.builtin.find_files { cwd = vim.fn.stdpath 'config' }
    -- end, { desc = '[S]earch [W]iki files' })
  end,
}
