#!/bin/zsh

# Vim in full screen mode
alias dvim='gvim +"call ToggleWindowSize()"'

alias dot='~/.dotfiles'
alias zen='~/.zen'

autoload zmv
alias mmv='noglob zmv -W'

# Global aliases
alias -g X='| xargs'
alias -g XX='| xargs -I {}'
alias -g X0='| xargs -0'
alias -g XX0='| xargs -0 -I {}'

# Suffix based aliases
alias -s gz=gunzip
alias -s bz2=bunzip2
alias -s zip=unzip
alias -s tar.gz='tar xzf'
alias -s tgz='tar xzf'
alias -s tar.bz2='tar xjf'

alias -s rb='ruby -rubygems'
alias -s py=python

# Avoid spelling corrections on certain commands
alias mv='nocorrect mv'
alias cp='nocorrect cp'
alias mkdir='nocorrect mkdir'

# Shortcut to aspell that sorts dictionary afterwards
function sp(){aspell -c "$@" && ~/.dotfiles/bin/sort_dictionary}

# Make git github aware
function git(){hub "$@"}

# Git shortcut
alias g='git'

alias lls='/usr/local/bin/gls -l --color=auto'
alias lst='/usr/local/bin/gls -ltr --color=auto'
alias ls='/usr/local/bin/gls --color=auto'

# Vim instead of vi
alias vi='vim'

# Quiet R
alias R='R --quiet --no-save --no-restore' 

# Quiet octave
alias octave='octave --quiet'

# Print grep results in color
alias grep='grep --color=auto'

alias sc='starcluster'
